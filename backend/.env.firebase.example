# Firebase 설정 예시
# Firebase를 사용하려면 이 파일을 .env로 복사하고 값을 설정하세요

# Database Provider 선택
DATABASE_PROVIDER=firestore  # postgresql | firestore

# Firebase Admin SDK
# Firebase Console → Project Settings → Service Accounts → Generate Private Key
FIREBASE_CREDENTIALS_PATH=/path/to/serviceAccountKey.json

# Firebase Web API Key (선택사항)
FIREBASE_API_KEY=your-firebase-api-key

# 기존 PostgreSQL 설정 (postgresql provider 사용 시)
# DATABASE_URL=postgresql://user:password@localhost:5432/tarot_db

# 기타 필수 설정
OPENAI_API_KEY=your-openai-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key
SECRET_KEY=your-secret-key-change-this-in-production
